package controller;

import model.User;
import view.MailboxView;

public class MailboxController {
    public MailboxController(User user, NavigationController navigationController, MailboxView mailboxView){
        //find the messages for the user and render in the table.
    }

}
